@model ExamViewModel


<div>
    <form asp-area="Applicant" asp-controller="Exam" asp-action="FinishExam">
        <input asp-for="ExamId" type="hidden"/>
        <input asp-for="Title" type="hidden" />
        <input asp-for="Description" type="hidden" />
        <button type="submit" class="btn btn-success">برای ثبت نتیجه آزمون اینجا کلیک کنید</button>
    </form>
</div>  